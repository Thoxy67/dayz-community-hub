[workspace]
members = [
    "dayz-community-hub-core",
    "dayz-community-hub-ui/src-tauri",
]
resolver = "2"

[workspace.package]
version = "0.4.0"
edition = "2024"
authors = ["thoxy"]

# ── Shared dependencies ────────────────────────────────────────────────────────

[workspace.dependencies]
# Async runtime
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "sync", "io-util", "process", "fs", "net"] }
tokio-stream = { version = "0.1.18", features = ["sync", "time"] }
futures-util = { version = "0.3", default-features = false, features = ["alloc"] }

# HTTP
reqwest = { version = "0.13.2", default-features = false, features = ["native-tls", "json", "query"] }

# Serialisation
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Error handling
thiserror = "2"

# Utilities
base64 = "0.22"
regex = "1"
which = "8.0.0"
sysinfo = "0.38.2"
chrono = { version = "0.4" }
clap = { version = "4", features = ["derive"] }

# Compression / archive
zip = { version = "8.1.0", default-features = false, features = ["deflate", "deflate64"] }
flate2 = "1"
tar = "0.4"

# DayZ A2S queries
async-a2s = { git = "https://git.thoxy.xyz/thoxy/async-a2s" }

# Internal crates
dayz-community-hub-core = { path = "dayz-community-hub-core" }

# ── Profile overrides ─────────────────────────────────────────────────────────

[profile.dev]
incremental = true

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
